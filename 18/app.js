// require('./firstModule.js');
var greet = require('./firstModule.js');

// greet(); // Not work cause its not the same context
//greet; // Not work cause its not the same context
greet();

greet.teste();
